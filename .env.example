# URL pública de la API (LHConsultoria-backend), usada desde el navegador
NEXT_PUBLIC_API_URL=http://localhost:4000/api
# URL pública de este sitio (ligas para compartir en redes)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Opcional: URL interna de la API para peticiones del servidor (p. ej. red de Docker en Dokploy)
API_INTERNAL_URL=http://localhost:4000/api
